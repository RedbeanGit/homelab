apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cilium
spec:
  interval: 15m
  timeout: 5m
  chart:
    spec:
      chart: cilium
      version: "1.19.1"
      sourceRef:
        kind: HelmRepository
        name: cilium
        namespace: flux-system
      interval: 5m
  releaseName: cilium
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  test:
    enable: true
  values:
    envoy:
      enabled: false
    hubble:
      ui:
        enabled: true
    kubeProxyReplacement: true
    k8sServiceHost: "${kubeapi_server_host}"
    k8sServicePort: ${kubeapi_server_port:=6443}
